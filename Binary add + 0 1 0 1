  #include <iostream>
#include <bitset>

std::string addBinary(int a, int b) {
    int c = a + b;
    std::string binary = std::bitset<8>(c).to_string();
    return binary;
}

int main() {
    std::cout << addBinary(4,5);
    return 0;
}
